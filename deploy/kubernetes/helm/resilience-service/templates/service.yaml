apiVersion: v1
kind: Service
metadata:
  name: {{ include "resilience-service.fullname" . }}
  labels:
    {{- include "resilience-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "resilience-service.selectorLabels" . | nindent 4 }}
