{{- if .Values.mfaService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "auth-platform.fullname" . }}-mfa-service
  labels:
    {{- include "auth-platform.labels" . | nindent 4 }}
    app.kubernetes.io/component: mfa-service
  annotations:
    kubernetes.io/change-cause: {{ .Values.mfaService.image.tag | quote }}
spec:
  {{- if not .Values.mfaService.autoscaling.enabled }}
  replicas: {{ .Values.mfaService.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "auth-platform.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: mfa-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 0
  template:
    metadata:
      labels:
        {{- include "auth-platform.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: mfa-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.mfaService.service.metricsPort }}"
        # Vault Agent Injector - Requirements 1.1
        vault.hashicorp.com/agent-inject: "true"
        vault.hashicorp.com/role: "mfa-service"
        vault.hashicorp.com/agent-inject-secret-config.env: "secret/data/auth-platform/config/mfa-service/default"
        vault.hashicorp.com/agent-inject-secret-totp.env: "secret/data/auth-platform/mfa/totp-config"
        # Linkerd injection - Requirements 3.2
        linkerd.io/inject: enabled
        config.linkerd.io/proxy-cpu-request: "100m"
        config.linkerd.io/proxy-memory-request: "20Mi"
    spec:
      serviceAccountName: {{ include "auth-platform.serviceAccountName" . }}-mfa-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        fsGroup: 65534
      containers:
        - name: mfa-service
          image: "{{ .Values.global.imageRegistry }}{{ .Values.mfaService.image.repository }}:{{ .Values.mfaService.image.tag }}"
          imagePullPolicy: {{ .Values.mfaService.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - name: grpc
              containerPort: {{ .Values.mfaService.service.grpcPort }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.mfaService.service.metricsPort }}
              protocol: TCP
          env:
            - name: SERVICE_NAME
              value: "mfa-service"
            - name: GRPC_PORT
              value: "{{ .Values.mfaService.service.grpcPort }}"
            - name: METRICS_PORT
              value: "{{ .Values.mfaService.service.metricsPort }}"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "{{ .Values.global.observability.tracing.endpoint }}"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "auth-platform.fullname" . }}-mfa-db
                  key: url
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          livenessProbe:
            grpc:
              port: {{ .Values.mfaService.service.grpcPort }}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            grpc:
              port: {{ .Values.mfaService.service.grpcPort }}
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.mfaService.resources | nindent 12 }}
      volumes:
        - name: tmp
          emptyDir: {}
      {{- with .Values.mfaService.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.mfaService.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
