# ArgoCD Application - Development Environment
# GitOps deployment configuration for dev

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auth-platform-dev
  namespace: argocd
  labels:
    environment: development
    app.kubernetes.io/part-of: auth-platform
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: auth-platform
  
  source:
    repoURL: https://github.com/auth-platform/auth-platform.git
    targetRevision: develop
    path: deploy/kubernetes/overlays/dev
  
  destination:
    server: https://kubernetes.default.svc
    namespace: auth-system-dev
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
    - group: autoscaling
      kind: HorizontalPodAutoscaler
      jsonPointers:
        - /spec/minReplicas
        - /spec/maxReplicas
  
  revisionHistoryLimit: 5
