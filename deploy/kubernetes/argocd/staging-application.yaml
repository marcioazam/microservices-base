# ArgoCD Application - Staging Environment
# GitOps deployment configuration for staging

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: auth-platform-staging
  namespace: argocd
  labels:
    environment: staging
    app.kubernetes.io/part-of: auth-platform
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: auth-platform
  
  source:
    repoURL: https://github.com/auth-platform/auth-platform.git
    targetRevision: release/*
    path: deploy/kubernetes/overlays/staging
  
  destination:
    server: https://kubernetes.default.svc
    namespace: auth-system-staging
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ApplyOutOfSyncOnly=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
    - group: autoscaling
      kind: HorizontalPodAutoscaler
      jsonPointers:
        - /spec/minReplicas
        - /spec/maxReplicas
  
  revisionHistoryLimit: 10
