---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: resiliencepolicies.resilience.auth-platform.github.com
spec:
  group: resilience.auth-platform.github.com
  names:
    kind: ResiliencePolicy
    listKind: ResiliencePolicyList
    plural: resiliencepolicies
    shortNames:
    - respol
    singular: resiliencepolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.targetRef.name
      name: Target
      type: string
    - jsonPath: .spec.circuitBreaker.enabled
      name: Circuit Breaker
      type: boolean
    - jsonPath: .spec.retry.enabled
      name: Retry
      type: boolean
    - jsonPath: .spec.timeout.requestTimeout
      name: Timeout
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: ResiliencePolicy is the Schema for the resiliencepolicies API.
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: ResiliencePolicySpec defines the desired state of ResiliencePolicy.
            properties:
              circuitBreaker:
                properties:
                  enabled:
                    default: false
                    type: boolean
                  failureAccrualMethod:
                    default: consecutive
                    enum:
                    - consecutive
                    type: string
                  failureThreshold:
                    default: 5
                    format: int32
                    maximum: 100
                    minimum: 1
                    type: integer
                required:
                - enabled
                - failureThreshold
                type: object
              rateLimit:
                properties:
                  burstSize:
                    format: int32
                    minimum: 1
                    type: integer
                  enabled:
                    default: false
                    type: boolean
                  requestsPerSecond:
                    format: int32
                    maximum: 100000
                    minimum: 1
                    type: integer
                required:
                - enabled
                - requestsPerSecond
                type: object
              retry:
                properties:
                  enabled:
                    default: false
                    type: boolean
                  maxAttempts:
                    default: 3
                    format: int32
                    maximum: 10
                    minimum: 1
                    type: integer
                  retryTimeout:
                    pattern: ^[0-9]+(ms|s|m)$
                    type: string
                  retryableStatusCodes:
                    pattern: ^[0-9x,]+$
                    type: string
                required:
                - enabled
                - maxAttempts
                type: object
              targetRef:
                properties:
                  name:
                    maxLength: 253
                    minLength: 1
                    type: string
                  namespace:
                    type: string
                  port:
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                required:
                - name
                type: object
              timeout:
                properties:
                  enabled:
                    default: false
                    type: boolean
                  requestTimeout:
                    pattern: ^[0-9]+(ms|s|m)$
                    type: string
                  responseTimeout:
                    pattern: ^[0-9]+(ms|s|m)$
                    type: string
                required:
                - enabled
                - requestTimeout
                type: object
            required:
            - targetRef
            type: object
          status:
            description: ResiliencePolicyStatus defines the observed state of ResiliencePolicy.
            properties:
              appliedToServices:
                items:
                  type: string
                type: array
              conditions:
                items:
                  description: Condition contains details for one aspect of the current state.
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastUpdateTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
