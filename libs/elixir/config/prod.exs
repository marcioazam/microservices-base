import Config

# Production-specific configuration
config :logger, level: :info

# Production settings are defined in config.exs defaults
# Override here if needed for specific production requirements
